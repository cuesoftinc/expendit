import React from "react";
import SettingsView from "./SettingsView";

/** B9 `/dashboard/settings` — Settings incl. rights (pages.md B9). */
export default function SettingsPage() {
  return <SettingsView />;
}
