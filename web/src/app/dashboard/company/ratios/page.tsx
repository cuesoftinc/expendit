import React from "react";
import RatiosView from "./RatiosView";

/** B6b `/dashboard/company/ratios` — Ratio grid (pages.md B6). */
export default function RatiosPage() {
  return <RatiosView />;
}
