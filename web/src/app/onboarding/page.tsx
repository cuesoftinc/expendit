import React from "react";
import OnboardingView from "./OnboardingView";

/** B0 `/onboarding` — first-run org create + AI consent (pages.md B0). */
export default function OnboardingPage() {
  return <OnboardingView />;
}
