import genStyles from '../CustomStyles';

const styles = {
  sectionContainer: `${genStyles.pagePad} vh-100 services-bg`,
  tabList: "bg-gray-300 p-4 rounded-md font-semibold mb-6 flex justify-around",
  bgBlack: 'bg-black text-white p-4',
  bgGray: 'bg-gray-300 text-black p-4',
  fluid_text: `${genStyles.fluid_text}`
}

export default styles